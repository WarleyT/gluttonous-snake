{
  "name": "gluttonous-snake",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:build": "node tools/build-cloudflare.mjs",
    "cf:dev": "npm run cf:build && wrangler dev",
    "cf:deploy": "npm run cf:build && wrangler deploy"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.0",
    "@netlify/functions": "^4.1.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260520.1",
    "wrangler": "^4.18.0"
  }
}
